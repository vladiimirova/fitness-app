export type LoginFormData = {
  email: string;
  password: string;
};

export type ProfileFormData = {
  name: string;
  age: string;
  weight: string;
  height: string;
  goal: string;
  activityLevel: string;
};